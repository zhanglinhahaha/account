<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app = "http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/manage"
        android:icon="@drawable/account_settings_custom"
        android:title="@string/menu_manage"
        app:showAsAction="always"
        android:visible="false"/>

    <item
        android:id="@+id/visibleItem"
        android:icon="@drawable/ic_action_eye_custom"
        android:title="@string/menu_manage"
        app:showAsAction="always"
        android:visible="false"/>

    <item
        android:id="@+id/importFile"
        android:icon="@drawable/account_add_dark"
        android:title="@string/menu_importFile"
        app:showAsAction="always"
        android:visible="false"/>

</menu>